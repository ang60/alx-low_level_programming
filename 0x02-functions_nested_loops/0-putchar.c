#include "main.h"
/**
 * main - Prints putchar.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	chary[8] = {'p', 'u', 't', 'c', 'h', 'a', 'r', '_'};
	int b;

	for(b = 0; b < 8; b++)
	{
		_putchar(output[b]);
	}
	_putchar('\n');
	return (0);
}
